import React, { FunctionComponent } from 'react';

type Homebtn = {
    GNRLCS: string,
}

export const Homebtn : FunctionComponent<Homebtn> = ({GNRLCS}) => {
    return (
        <div>Homebtn</div>
    )
}